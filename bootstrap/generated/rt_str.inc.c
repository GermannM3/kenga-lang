/* GENERATED by kenga/emit/rt_str.kenga — do not hand-edit */
static void k_clamp_range(int64_t n, int64_t *start, int64_t *end) {
  if (*start < 0) *start = 0;
  if (*start > n) *start = n;
  if (*end > n) *end = n;
  if (*end < *start) *end = *start;
}

static char *k_slice(const char *in, int64_t start, int64_t end) {
  size_t n;
  size_t len;
  char *o;
  if (!in) in = "";
  n = strlen(in);
  k_clamp_range((int64_t)n, &start, &end);
  len = (size_t)(end - start);
  o = (char *)malloc(len + 1);
  if (!o) die("oom");
  if (len) memcpy(o, in + (size_t)start, len);
  o[len] = 0;
  return o;
}

static int64_t k_index_of(const char *s, const char *p) {
  size_t n, m, i;
  if (!s) s = "";
  if (!p) p = "";
  n = strlen(s);
  m = strlen(p);
  if (m == 0) return 0;
  if (m > n) return -1;
  for (i = 0; i + m <= n; i++) {
    if (memcmp(s + i, p, m) == 0) return (int64_t)i;
  }
  return -1;
}

static int64_t k_starts_with(const char *s, const char *p) {
  size_t m;
  if (!s) s = "";
  if (!p) p = "";
  m = strlen(p);
  if (m > strlen(s)) return 0;
  return memcmp(s, p, m) == 0 ? 1 : 0;
}
