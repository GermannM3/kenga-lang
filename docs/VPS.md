# ??? ?? VPS

???? ???????, long poll. ????????? HTTP-?????? ? nginx ?? ?????:
Telegram ??? ???????? ? `getUpdates`.

????? ?? ?????? ????? ????. ??????? ???? ? ??????, ????? DuckDuckGo / Wikipedia
???? ?? ?????? ? ??? ?????? ??? `search:`. ???????: `after_ms` + `on` + `pump`
(??. `examples/async_tick.kenga`). ???? `http_request` ???? long poll, VM
?????? `pump`.

?????? ????: `minds/tg_memory.txt`. ??: `minds/tg_prophet.km` (?? ? git).
`remember: ????` ????? ? ???? ? ???? ???????.

## 1. ??????

Ubuntu 22.04 / Debian 12, x86_64. ????? `gcc` ??? `clang`, git.

```bash
sudo apt update
sudo apt install -y build-essential git curl
```

????????? 443 ?? `api.telegram.org`, `api.duckduckgo.com`, `*.wikipedia.org`.
???????? ????? ??? long poll ?? ?????.

## 2. ??????

```bash
git clone https://github.com/GermannM3/kenga-lang.git
cd kenga-lang
bash bootstrap/build.sh
./bootstrap/bin/kenga-lite run examples/selfhost/net_lite.kenga
```

?? Linux ???????? ??? `.exe`: `bootstrap/bin/kenga-lite`.

## 3. ?????

? BotFather: `/newbot`. ????? ?? ????? ? git.

```bash
sudo mkdir -p /etc/kenga
sudo tee /etc/kenga/telegram.env >/dev/null <<'EOF'
TELEGRAM_BOT_TOKEN=123456:ABC-DEF
EOF
sudo chmod 600 /etc/kenga/telegram.env
```

????????:

```bash
set -a
. /etc/kenga/telegram.env
set +a
mkdir -p minds
./bootstrap/bin/kenga-lite run examples/telegram_bot.kenga
```

? ???? ???? `/start`. ?????????: Ctrl+C.

## 4. systemd

????: `scripts/kenga-telegram.service`.

```bash
sudo cp scripts/kenga-telegram.service /etc/systemd/system/
sudo nano /etc/systemd/system/kenga-telegram.service
# WorkingDirectory= ?????? ????
# ExecStart= .../bootstrap/bin/kenga-lite run examples/telegram_bot.kenga

sudo systemctl daemon-reload
sudo systemctl enable --now kenga-telegram
sudo systemctl status kenga-telegram
journalctl -u kenga-telegram -f
```

????? `git pull` + `bash bootstrap/build.sh`:

```bash
sudo systemctl restart kenga-telegram
```

`minds/` ?????? ???? ? `WorkingDirectory` ? ???????? ?????????.

```bash
mkdir -p minds
```

## 5. ???? ???? ???

- webhook + nginx — ?????, ????? ????? `http_listen`.
- `screen`/`tmux` ?????? systemd: ??????? ????? ?? ssh-logout.

## 6. ???? api.telegram.org ?? ???????????

? VPS ??? ????, ??? ? ????????? IP. ????????: `curl -I https://api.telegram.org`.
