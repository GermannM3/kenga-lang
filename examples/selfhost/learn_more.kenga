fn main() -> i64 {
    let m = memory_config(5, 16, 8);
    let loss = learn(m, [1, 2, 3], [2, 3, 4]);
    let p = predict(m, [1, 2, 3]);
    assert(len(p) == 3);
    println("learn more ok");
    return 0;
}
