// Dialect bits for bc_src_c: %, &&, ||, !, block comments.

fn main() -> i64 {
    assert(10 % 3 == 1);
    assert((1 && 0) == 0);
    assert((0 || 1) == 1);
    assert(!0 == 1);
    assert(1 + /* c */ 2 == 3);
    println("bc logic ok");
    return 0;
}
