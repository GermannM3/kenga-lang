// Lists via bc_src_c (parse → bytecode → C VM).

fn sum(xs) {
    let s = 0;
    let i = 0;
    while i < len(xs) {
        s = s + xs[i];
        i = i + 1;
    }
    return s;
}

fn main() {
    let xs = [1, 2, 3, 4];
    xs[0] = 10;
    println(sum(xs));
    return 0;
}
