// pico seed pow: pow(2, 10) = 1024
fn pow(a: i64, b: i64) -> i64 {
    let r = 1;
    let i = 0;
    while i < b {
        r = r * a;
        i = i + 1;
    }
    return r;
}
fn main() -> i64 {
    let y = pow(2, 10);
    println(y);
    return 0;
}
