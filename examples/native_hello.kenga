// C-backend smoke target (subset language)

fn main() -> i64 {
    println("hello native");
    let x: i64 = 21;
    let y: i64 = 2;
    println(x * y);
    if x > 10 {
        println(1);
    } else {
        println(0);
    }
    return 0;
}
